{
  "subsystem": "SelfCheck",
  "title": "Regression Suite",
  "targets": { "cfg": "ckf.hardmode.cfg", "json": "ckf.hardmode.d/selfcheck.json", "legacyJson": null },
  "enable": { "cfg": "Slices.SelfCheck" },

  "doc": [
    "CKF Hard Mode - regression suite.",
    "",
    "Reads a list of rows by id at startup and checks their columns against",
    "values written down in advance, reporting PASS/FAIL per column. This is how",
    "the retuning arithmetic gets re-checked without a CKF Data Dump sweep - a",
    "sweep and row cloning cannot be used together (Run 42).",
    "",
    "THE EXPECTATIONS FILE IS NOT A SCHEMA INPUT. Columns are",
    "Table,Id,Column,Expect,Note, one row per assertion, and it is bulk table",
    "authoring in the same sense as ckf.hardmode.rules.json - it belongs to the",
    "spreadsheet, not to a generator. Leave Expect blank to report a value",
    "without judging it.",
    "",
    "IT IS ONLY AS GOOD AS ITS BASELINE, and it is OFF by default because of",
    "that - the one feature switch in the mod that is. Turn it on for a",
    "verification launch after a retune, regenerate the expectations, read the",
    "block, turn it back off.",
    "",
    "Run56 reported 58 of 102 failing. Two different causes, and separating them",
    "took reading all 102 rather than sampling: [measured]",
    "",
    "  Section R/E, 79 rows - a STALE BASELINE. selfcheck.csv was written 27",
    "  hours before ckf.hardmode.d/*.csv and never refreshed. The mod matched the",
    "  overlay CSVs on all 65 rows an overlay covers, with zero disagreements.",
    "  Re-baselined 2026-08-31.",
    "",
    "  Section V, 23 rows - ORPHANED TESTS, not drift. They assert against",
    "  rule-engine operator scaffolding (ids 22201-22206, 22301, 990000, 990001)",
    "  that appears ZERO times in ckf.hardmode.rules.json, with no .tsv overlay",
    "  on disk. Eight read 0, which is SelfCheck saying 'no such row'. Parked as",
    "  comments, not deleted: they cover the ENGINE - set, multiply, add,",
    "  clampMin, clampMax, operator order, later-file-wins, inheritance on insert",
    "  - and that coverage exists nowhere else. Restoring it means re-creating",
    "  the test rules."
  ],

  "uiDoc": [
    "A startup check. It reads a list of rows by id and compares their columns against values written down in advance, reporting pass or fail for each one. It is how you confirm a retune landed without running a full data sweep.",
    "",
    "To use it, turn it on for one launch after a retune, read the block it prints, then turn it back off.",
    "",
    "The expectations are a plain table - table, id, column, expected value, note - one row per check, edited in a spreadsheet. Leave the expected value blank and it reports what it found without judging it, which is a quick way to read a column you are unsure about.",
    "",
    "It is only as good as the values you wrote down. An expectations file that has not been refreshed since your last retune reports failures that are only the file being out of date."
  ],

  "fields": [
    {
      "path": "Slices.SelfCheck", "in": "cfg", "type": "bool",
      "default": false, "ui": "form",
      "label": "Enable Regression Suite",
      "doc": "Slice toggle. One key per slice in ckf.hardmode.cfg, because a gate cannot live inside the file it gates and the .cfg is the one file that must parse before any other loading happens (split-config-into-toggleable-slices design.md section 3). Off means the slice never enters RulePlan. THIS IS THE GATE. Slices.Init binds every key in Binds.All (Slices.cs) and is called from Plugin.Load (Plugin.cs), above the master-switch bail-out, so BepInEx writes the line whether the mod is on or off. CONFIRMED ON DISK 2026-09-13, after a launch: ckf.hardmode.cfg is 3,238 bytes carrying [General] and [Slices] and all 43 keys, and check_schema.py --game printed \"0 problem(s). 43 cfg key(s) on disk, 43 declared across 43 schema file(s).\" at rc 0 [measured, Logs\\gates-phase1b.txt]. The tag on this sentence used to sit before that run: it read \"[measured 2026-09-13]\" when what had been measured was the C#, not the file. The file has now been measured too. CORRECTION, 2026-09-13: this doc used to say \"the section's own \\\"enabled\\\" field is still on disk and is still the gate the plugin reads\" and \"NOT ON DISK ... check_schema.py reports it MISSING\". Both were true when written and are false now. The section's \"enabled\" field is deleted from this schema and the gate is this key. The RETIRED KEY IS STILL PARSED, into a report-only bool? RetiredEnabled ([JsonPropertyName(\"enabled\")]), because four subsystems refuse a whole section on an unknown key (ConfigDoc.ReadSection, ConfigDoc.cs) and an upgraded document would otherwise switch OFF. Slices.ReportRetiredGate (Slices.cs) names it: Warning normally, Error when the retired value is false and this key is not. Nothing branches on it -- grep RetiredEnabled finds one declaration and one report call per subsystem and no read. It is a migration report, not a second gate. Defaults FALSE, matching the \"selfcheck\" section's own enabled field, which is the one default among the nine that is not true [measured 2026-09-13, schema/selfcheck.schema.json].",
      "uiDoc": "Turn this off and this part of the mod does nothing; the game uses its own values. The master switch on the first page turns off everything at once."
    },
    {
      "path": "file", "in": "json", "type": "string",
      "default": "", "ui": "form",
      "enabledBy": "enabled",
      "absent": "Blank means ckf.hardmode.selfcheck.csv beside the config file.",
      "label": "Expectations file",
      "doc": "The expectations file. Blank means ckf.hardmode.selfcheck.csv beside this config. Columns: Table,Id,Column,Expect,Note. Leave Expect blank to report the value without judging it."
    },
    {
      "path": "output", "in": "json", "type": "string",
      "default": "", "ui": "form",
      "enabledBy": "enabled",
      "absent": "Blank means BepInEx/ckf-hardmode/selfcheck.csv.",
      "label": "Result table path",
      "doc": "Where to write the result table. Blank means BepInEx/ckf-hardmode/selfcheck.csv. The log carries the same lines."
    }
  ]
}
